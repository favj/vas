<UserControl
    x:Class="TyMetrix360.App.View.Template.ConfirmationTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TyMetrix360.App.View.Template"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="#FFFCD9">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Foreground="#000000" FontSize="20" FontWeight="Bold">
            <Run Text="Inv #"/>
            <Run Text="{Binding InvoiceNumber}"/>
        </TextBlock>
        <TextBlock Grid.Column="1" Text="{Binding NetAmount}" Foreground="#000000" FontSize="20" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
